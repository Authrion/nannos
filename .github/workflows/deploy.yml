# Собирает файлы окружения из секретов, копирует их на машину и
# перезапускает стек. Пароли нигде не сохраняются: машина Actions
# одноразовая и удаляется вместе с содержимым.
name: deploy

on:
  workflow_run:
    workflows: [build]
    types: [completed]
    branches: [main, deploy/aws]
  workflow_dispatch:
    inputs:
      tag:
        description: 'SHA коммита или latest'
        default: latest

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    steps:
      - uses: actions/checkout@v4

      - name: Собрать конфиги и env-файлы
        env:
          PUBLIC_HOST: ${{ secrets.PUBLIC_HOST }}
        run: |
          mkdir -p out
          cp deploy/docker-compose.prod.yml \
             deploy/Caddyfile \
             deploy/nginx.conf \
             deploy/litellm-config.yaml \
             deploy/realm-export.json \
             out/

          cat > out/.env <<ENVEOF
          PUBLIC_HOST=${{ secrets.PUBLIC_HOST }}
          TAG=${{ inputs.tag || github.event.workflow_run.head_sha || 'latest' }}
          KEYCLOAK_ADMIN_PASSWORD=${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}
          KC_DB_HOST=${{ secrets.KC_DB_HOST }}
          KC_DB_NAME=${{ secrets.KC_DB_NAME }}
          KC_DB_USER=${{ secrets.KC_DB_USER }}
          KC_DB_PASSWORD=${{ secrets.KC_DB_PASSWORD }}
          ENVEOF

          # printf, а не echo: у echo разное поведение с обратными слешами,
          # и пароль, содержащий "\", может исказиться.
          printf '%s\n' "${{ secrets.ORCHESTRATOR_ENV }}"    > out/orchestrator.env
          printf '%s\n' "${{ secrets.CONSOLE_BACKEND_ENV }}" > out/console-backend.env
          printf '%s\n' "${{ secrets.LITELLM_ENV }}"         > out/litellm.env

          chmod 600 out/*.env out/.env

      - name: Скопировать на машину
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_KEY }}
          source: "out/*"
          target: /opt/nannos
          strip_components: 1
          overwrite: true

      - name: Перезапустить стек
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_KEY }}
          command_timeout: 15m
          script: |
            set -e
            cd /opt/nannos
            echo "${{ secrets.GITHUB_TOKEN }}" \
              | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d --remove-orphans
            docker image prune -f
            echo '--- состояние ---'
            docker compose -f docker-compose.prod.yml ps
